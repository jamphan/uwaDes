/*
Short notes.
	* implements the macro language to transform C programs before they are compiled
	
*/


/* ----------------------------------------------------------------------------
 * Useful preprocessors commands
 * ----------------------------------------------------------------------------*/
        
#define MAX(a,b) ((a) > (b) ? a : b)
#define MIN(a,b) ((a) < (b) ? a : b)